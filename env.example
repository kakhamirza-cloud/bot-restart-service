# Railway CLI Configuration
# Note: Railway CLI must be installed and user must be logged in
# Run: railway login

# Service Names (use service names instead of IDs for Railway CLI)
GLYPHS_BOT_GAME_SERVICE_NAME=glyphs-bot-game
GLYPHS_BOT_1_SERVICE_NAME=glyphs-bot-1

# Instructions:
# 1. Install Railway CLI: npm install -g @railway/cli
# 2. Login to Railway: railway login
# 3. Set the correct service names above
# 4. Deploy this service to Railway
